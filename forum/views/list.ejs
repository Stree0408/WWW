<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="/main.css" rel="stylesheet">

</head>

<body class="grey-bg">
    <!-- <%= JSON.stringify(글목록)%> -->
    <%- include ('nav.ejs') %>

        <div class="white-bg">
            <% for (let i=0; i<글목록.length; i++) { %>
                <div class="list-box">
                    <!-- <%= JSON.stringify(글목록[i]._id)%> -->
                    <h4>
                        <a href="/detail/<%= 글목록[i]._id %>">
                            <%= 글목록[i].title %>
                        </a>
                        <a href="/edit/<%= 글목록[i]._id %>">✏️</a>
                        <span class="delete" data-id="<%= 글목록[i]._id %>">🗑️</span>
                    </h4>
                    <p>
                        <%= 글목록[i].content %>
                    </p>
                </div>
                <% } %>


        </div>


        <a href="/list/next/<%= 글목록[글목록.length-1]._id %>">다음</a>


        <script>
            // document.querySelectorAll('.delete')[0].addEventListener('click', function () {
            //     fetch('/delete', {
            //         method: 'post',      // method delete라고 하면, body 잘 안먹을 때 있음
            //         headers: { 'Content-Type': 'application/json' },
            //         body: JSON.stringify({ id: '<%= 글목록[0]._id %>' })
            //     })
            // })
            for (let i = 0; i < '<%= 글목록.length %>'; i++) {
                document.querySelectorAll('.delete')[i].addEventListener('click', function (e) {
                    fetch('/delete?docid=' + e.target.dataset.id, {
                        method: 'delete',
                    })
                        .then((r) => r.text())
                        .then((r) => {
                            e.target.parentElement.parentElement.style.display = 'none';
                        })
                })
            }



        </script>
</body>

</html>